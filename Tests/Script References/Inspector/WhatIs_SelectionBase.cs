using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[SelectionBase] //Add SelectionBase to preferred selection when normally a child would be selected
//Example: An empty Parent with a Manager script containing [SelectionBase] on it holds a cube as a child. 
//Click the cube, in scene, and the parent object will be selected
public class WhatIs_SelectionBase : MonoBehaviour
{

}
